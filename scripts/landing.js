
var animatePoints = function() {
  var revealPoint = function () {
    $(this).css({
      opacity: 1,
      transofrm: 'scaleX(1) translateY(0)'
    });
  };

  $.each($('.point'), revealPoint);
};

//  Paste below 


//
  });
});
